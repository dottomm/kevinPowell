---
import {ViewTransitions} from 'astro:transitions';
import {slide} from 'astro:transitions';
import { fade } from 'astro:transitions';
import Layout from '../layouts/Layout.astro';
import { getEntryBySlug } from "astro:content";

import CTA from '../components/CTAButton.astro'

const homepage = await getEntryBySlug("homepage", "index");
const { banner, vision, mission, invite, feature, services, journey, primary, workflow, call_to_action } = homepage.data;

---

<Layout title="Partnership for Effective Practices in Transition and Inclusion">
  <main transition:animate={fade({duration: '0.6s'})}>
   

<section class="full-width-split-screen" >	
  <img src="/images/small-GettyImages-514561326.webp"  alt="two children playing with blocks" width="100%" height="100%"/>
    <div class="secondary" >
    <p>
    {mission.content}
    </p>
    {
    banner?.button?.enable && (
      <a class="button" href={banner.button.link}>
      {banner.button.label}
      </a>
    )
    }
    </div>
</section>

<section class="full-width-split-screen" >
  
  <div class="accent">
    <div > 
      {
      journey.button?.enable && (
        <a class="button" href={journey.button.link}>
        {journey.button.label}
        </a>
      )
      } 
  </div>
    <p>{journey.content}</p>
  </div>
  <img src='/images/small-GettyImages-1483125813.webp' alt='young child working with teacher' width="100%" height="100%">
</section>


  <!--
    <section class="full-width-split-screen">
  <div class="wrapper">
	<h2>{invite.title}</h2>
  <p>Booger</p>
</section>
   -->
 
  <section class="full-width-split-screen">
  
    <img src="/images/small-GettyImages-920503140.webp" alt="Children on playground" width="100%" height="100%">

  
  <div class="accent" >
  <p>{invite.content}</p>  
  <p>   {
      call_to_action?.button?.enable && (
        <a class="button" href={call_to_action.button.link}>
        {call_to_action.button.label}
        </a>
      )
      }</p>
       
	
  </div>
</section>
                <!--
                  <section class="">
                <div class="wrapper">
                  <h2>{mission.title}</h2>
                <p>{mission.content}</p>
                <p>accent</p>

                </div>
              </section>
                -->

<!--
  <section class="full-width-split-screen">

  <img src='/images/GettyImages-985125732.jpg' alt='' />

</section>
 -->

	</main>
</Layout>

